# 09_字典操作

```python
person = {
    "name": "Alice",
    "age": 25,
    "city": "Beijing"
}

# 访问值
print(person["name"])      # Alice
print(person.get("age"))   # 25

# 添加/修改
person["job"] = "Engineer"

# 遍历
for key, value in person.items():
    print(f"{key}: {value}")
```
