# 首屏加载时间优化

## 首屏加载时间指标

| 指标 | 说明 | 目标值 |
|------|------|--------|
| FCP (First Contentful Paint) | 首次内容绘制 | < 1.8s |
| LCP (Largest Contentful Paint) | 最大内容绘制 | < 2.5s |
| TTI (Time to Interactive) | 可交互时间 | < 3.8s |
| TBT (Total Blocking Time) | 总阻塞时间 | < 200ms |

## 优化策略

### 1. 代码分割

```javascript
// 路由级别的代码分割
const Home = () => import('./views/Home.vue')
const About = () => import('./views/About.vue')

// 懒加载组件
const HeavyComponent = () => import('./HeavyComponent.vue')
```

```javascript
// Webpack 配置
optimization: {
  splitChunks: {
    chunks: 'all',
    cacheGroups: {
      vendor: {
        test: /[\\/]node_modules[\\/]/,
        name: 'vendors',
        chunks: 'all',
      },
    },
  },
}
```

### 2. 图片优化

```vue
<template>
  <!-- 使用 WebP 格式 -->
  <img src="image.webp" alt="描述">

  <!-- 懒加载 -->
  <img src="image.webp" loading="lazy" alt="描述">

  <!-- 响应式图片 -->
  <picture>
    <source srcset="image-small.webp 480w, image-medium.webp 800w" media="(max-width: 800px)">
    <source srcset="image-large.webp 1200w" media="(min-width: 801px)">
    <img src="image-medium.webp" alt="描述" loading="lazy">
  </picture>
</template>
```

```javascript
// 压缩图片
import Compress from 'compressorjs'

const compressor = new Compressor(file, {
  quality: 0.6,
  maxWidth: 1920,
  maxHeight: 1080,
  mimeType: 'image/webp',
})
```

### 3. 防止阻塞

```vue
<!-- 异步加载脚本 -->
<script src="analytics.js" defer></script>

<!-- 非关键 CSS -->
<link rel="preload" href="style.css" as="style" onload="this.rel='stylesheet'">
```

```javascript
// 优化 script 标签
<script>
// 将非关键 JS 放在页面底部
</script>
```

### 4. CDN 加速

```javascript
// 配置 CDN 域名
const cdnUrl = 'https://cdn.example.com'

// 引用静态资源
<script src="https://cdn.example.com/vue.js"></script>
<link rel="stylesheet" href="https://cdn.example.com/style.css">
```

```nginx
# Nginx 配置
server {
  listen 80;
  server_name cdn.example.com;

  root /var/www/cdn;
  expires 1y;
  add_header Cache-Control "public, immutable";
}
```

### 5. 代码压缩

```javascript
// Vite 配置
export default defineConfig({
  build: {
    minify: 'terser',
    terserOptions: {
      compress: {
        drop_console: true,
        drop_debugger: true,
      },
    },
    // 分包策略
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['vue', 'vue-router'],
          utils: ['axios', 'lodash'],
        },
      },
    },
  },
})
```

### 6. Gzip / Brotli 压缩

```nginx
# Nginx 启用 Gzip
gzip on;
gzip_types text/plain text/css application/json application/javascript text/xml application/xml;
gzip_min_length 1000;

# Nginx 启用 Brotli
brotli on;
brotli_types text/plain text/css application/json application/javascript;
brotli_comp_level 6;
```

### 7. 缓存策略

```javascript
// Service Worker 缓存
const CACHE_NAME = 'v1'

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => {
      return cache.addAll([
        '/',
        '/index.html',
        '/css/style.css',
        '/js/app.js',
      ])
    })
  )
})

self.addEventListener('fetch', (event) => {
  event.respondWith(
    caches.match(event.request).then((response) => {
      return response || fetch(event.request)
    })
  )
})
```

```javascript
// HTTP 缓存头
Cache-Control: public, max-age=31536000, immutable
```

### 8. 预加载关键资源

```html
<!-- 预加载关键 CSS -->
<link rel="preload" href="critical.css" as="style">

<!-- 预连接域名 -->
<link rel="preconnect" href="https://api.example.com">
<link rel="preconnect" href="https://cdn.example.com" crossorigin>

<!-- 预获取资源 -->
<link rel="prefetch" href="/next-page.js">
<link rel="preload" href="/heavy-chart.js" as="script">
```

### 9. 减少 HTTP 请求

```javascript
// 合并文件
// CSS 合并
<link rel="stylesheet" href="all.css">

// JS 合并
<script src="all.js"></script>

// 图标合并
<svg>
  <!-- 图标定义 -->
</svg>
```

### 10. 减少 JS 执行时间

```javascript
// 节流函数
function throttle(fn, delay) {
  let lastTime = 0
  return function(...args) {
    const now = Date.now()
    if (now - lastTime >= delay) {
      lastTime = now
      fn.apply(this, args)
    }
  }
}

// 使用 requestIdleCallback
if ('requestIdleCallback' in window) {
  requestIdleCallback(() => {
    // 低优先级任务
  })
}
```

### 11. 骨架屏

```vue
<template>
  <div class="skeleton">
    <div class="skeleton-item"></div>
    <div class="skeleton-item"></div>
    <div class="skeleton-item"></div>
  </div>
</template>

<style>
.skeleton {
  background: #f0f0f0;
  animation: pulse 1.5s infinite;
}

@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}
</style>
```

### 12. 服务器端渲染 (SSR)

```javascript
// Nuxt.js SSR
export default {
  async asyncData({ $axios }) {
    const data = await $axios.get('/api/data')
    return { data }
  }
}
```

## 优化检查清单

### 文件层面
- [ ] 启用 Gzip/Brotli 压缩
- [ ] 压缩图片为 WebP 格式
- [ ] 使用 CDN 加速
- [ ] 代码分割
- [ ] 合并 CSS/JS 文件

### 网络层面
- [ ] 启用 HTTP/2
- [ ] 设置缓存策略
- [ ] 预加载关键资源
- [ ] 减少请求数量
- [ ] 预连接域名

### 渲染层面
- [ ] 使用骨架屏
- [ ] 减少阻塞渲染的 JS/CSS
- [ ] 异步加载非关键资源
- [ ] 使用 SSR/SSG

### 监控层面
- [ ] 使用 Lighthouse 评分
- [ ] 监控真实用户性能 (RUM)
- [ ] 设置性能告警

## 工具推荐

| 工具 | 用途 |
|------|------|
| Lighthouse | 性能分析 |
| WebPageTest | 详细性能测试 |
| Chrome DevTools | 性能分析 |
| SpeedCurve | 性能监控 |
| Sentry | 错误监控 |

## 总结

```
首屏加载优化优先级：
1. 路由级代码分割 - 减少首屏 JS 体积
2. 图片优化 - WebP 格式 + 懒加载
3. CDN 加速 - 静态资源加速
4. Gzip/Brotli - 压缩传输数据
5. 缓存策略 - 减少重复请求
6. 预加载 - 提前加载关键资源
7. SSR - 服务端渲染加快首屏
```

**核心思路：减少传输数据量 + 加快传输速度 + 减少渲染等待**
