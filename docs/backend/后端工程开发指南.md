# 后端工程开发指南

## 一、需求分析与设计阶段

### 1.1 需求收集与分析

```
用户需求 → 业务需求 → 功能需求 → 非功能需求
    ↓           ↓           ↓            ↓
  用户故事    业务流程    API接口    性能/安全/可扩展性
```

**关键产出物：**
- **需求文档（PRD）**：描述业务目标、用户场景、功能列表
- **流程图**：业务流程可视化
- **用例图**：用户与系统的交互

### 1.2 技术选型

| 分类 | 常见选择 | 选择依据 |
|------|----------|----------|
| **语言** | Java, Go, Python, Node.js | 团队经验、性能需求、生态 |
| **框架** | Spring Boot, Gin, FastAPI, Express | 生态成熟度、社区活跃度 |
| **数据库** | MySQL, PostgreSQL, MongoDB, Redis | 数据结构、查询复杂度 |
| **消息队列** | Redis, RabbitMQ, Kafka | 吞吐量、可靠性要求 |
| **缓存** | Redis, Memcached | 读写比例、数据结构需求 |

**技术选型决策矩阵：**

```
┌─────────────────────────────────────────────────────────────┐
│                    技术选型决策                             │
├──────────────┬──────────────┬──────────────┬──────────────┤
│   维度       │   高要求     │   中要求     │   低要求     │
├──────────────┼──────────────┼──────────────┼──────────────┤
│ 性能/并发    │    Go/C++    │   Java/Go    │   Python     │
│ 生态成熟度   │  Spring Boot │   Gin/FastAPI│   Express    │
│ 数据复杂度   │ PostgreSQL   │   MySQL      │   SQLite     │
│ 实时性要求   │   Kafka      │  RabbitMQ    │   Redis      │
└──────────────┴──────────────┴──────────────┴──────────────┘
```

### 1.3 架构设计

**分层架构：**

```
┌─────────────────────────────────────────────────────────────┐
│                    后端架构分层                             │
├──────────────┬──────────────┬──────────────┬──────────────┤
│  Controller  │   Service    │   Repository │   Database   │
│  (API层)     │  (业务层)    │   (数据层)   │   (存储层)   │
│  ┌─────────┐ │  ┌─────────┐ │  ┌─────────┐ │  ┌───────┐  │
│  │ REST API│ │  │业务逻辑 │ │  │数据访问 │ │  │ MySQL │  │
│  │ 路由控制│ │  │事务管理 │ │  │ORM映射  │ │  │Redis  │  │
│  │ 参数校验│ │  │权限校验 │ │  │缓存管理 │ │  │MongoDB│  │
│  └─────────┘ │  └─────────┘ │  └─────────┘ │  └───────┘  │
└──────────────┴──────────────┴──────────────┴──────────────┘
```

**核心设计原则：**
- **单一职责**：每个模块只负责一件事
- **依赖倒置**：高层模块不依赖低层模块，都依赖抽象
- **开闭原则**：对扩展开放，对修改关闭
- **接口隔离**：客户端不依赖它不需要的接口

---

## 二、项目结构设计

### 2.1 标准目录结构

```
backend/                              # Maven 后端应用根目录
  ├── src/
  │   └── main/
  │       ├── java/
  │       │   └── com/example/app/
  │       │       ├── controller/     # REST API 控制层
  │       │       │   ├── UserController.java
  │       │       │   └── OrderController.java
  │       │       ├── service/        # 业务逻辑层
  │       │       │   ├── UserService.java
  │       │       │   └── impl/
  │       │       │       └── UserServiceImpl.java
  │       │       ├── repository/     # 数据访问层 (DAO)
  │       │       │   └── UserRepository.java
  │       │       ├── entity/         # 数据库实体
  │       │       │   └── User.java
  │       │       ├── dto/            # 数据传输对象
  │       │       │   ├── request/
  │       │       │   └── response/
  │       │       ├── config/         # 配置类
  │       │       │   ├── RedisConfig.java
  │       │       │   └── SecurityConfig.java
  │       │       ├── exception/      # 异常处理
  │       │       │   ├── GlobalExceptionHandler.java
  │       │       │   └── BusinessException.java
  │       │       ├── util/           # 工具类
  │       │       │   └── JwtUtil.java
  │       │       └── Application.java  # Spring Boot 启动类
  │       └── resources/
  │           ├── application.yml      # 应用配置
  │           └── schema.sql          # 数据库初始化脚本
  └── pom.xml                         # Maven 依赖管理
```

### 2.2 目录职责说明

| 目录 | 职责 | 状态 |
|------|------|------|
| `controller` | 处理 HTTP 请求，参数校验，调用 Service | 对外暴露 |
| `service` | 业务逻辑处理，事务管理 | 内部调用 |
| `repository` | 数据库访问，ORM 操作 | 内部调用 |
| `entity` | JPA 实体类，映射数据库表 | 内部使用 |
| `dto` | 请求/响应数据结构 | 数据传输 |
| `config` | Spring 配置类 | 配置管理 |
| `exception` | 全局异常处理 | 错误处理 |
| `util` | 工具类 | 通用功能 |

---

## 三、数据库设计

### 3.1 设计流程

```
需求分析 → 概念模型 → 逻辑模型 → 物理模型 → DDL
    ↓           ↓           ↓           ↓
  业务理解    ER图设计    表结构设计  SQL语句
```

### 3.2 实体关系图（ER图）

```
┌──────────┐        ┌──────────┐        ┌──────────┐
│  User    │ 1:N    │  Order   │ 1:N    │ OrderItem│
├──────────┤◄───────├──────────┤◄───────├──────────┤
│ id       │        │ id       │        │ id       │
│ username │        │ user_id  │        │ order_id │
│ email    │        │ total    │        │ product_id│
│ password │        │ status   │        │ quantity │
│ created  │        │ created  │        │ price    │
└──────────┘        └──────────┘        └──────────┘
       │                  │                  │
       │                  │                  │
       ▼                  ▼                  ▼
  ┌──────────┐        ┌──────────┐        ┌──────────┐
  │ Address  │        │ Product  │        │ Category │
  ├──────────┤        ├──────────┤        ├──────────┤
  │ user_id  │        │ id       │        │ id       │
  │ city     │        │ name     │        │ name     │
  │ street   │        │ price    │        │ parent_id│
  └──────────┘        │ stock    │        └──────────┘
                      │ category_id│
                      └──────────┘
```

### 3.3 表设计规范

**用户表（users）：**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 用户ID |
| username | VARCHAR(50) | NOT NULL, UNIQUE | 用户名 |
| email | VARCHAR(100) | NOT NULL, UNIQUE | 邮箱 |
| password | VARCHAR(255) | NOT NULL | 加密密码 |
| nickname | VARCHAR(50) | NULL | 昵称 |
| phone | VARCHAR(20) | NULL | 手机号 |
| status | TINYINT | DEFAULT 1 | 状态：0禁用，1启用 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**订单表（orders）：**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 订单ID |
| user_id | BIGINT | FOREIGN KEY → users(id) | 用户ID |
| order_no | VARCHAR(64) | UNIQUE | 订单编号 |
| total_amount | DECIMAL(10,2) | NOT NULL | 订单总额 |
| status | TINYINT | DEFAULT 0 | 状态：0待支付，1已支付，2已发货，3已完成 |
| shipping_address | TEXT | NULL | 收货地址 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

### 3.4 索引设计原则

```sql
-- 用户表索引
CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_users_username ON users(username);

-- 订单表索引
CREATE INDEX idx_orders_user_id ON orders(user_id);
CREATE INDEX idx_orders_status ON orders(status);
CREATE INDEX idx_orders_created_at ON orders(created_at);
```

**索引设计要点：**
- **主键索引**：必须有
- **唯一索引**：唯一约束字段
- **普通索引**：查询频繁的字段
- **复合索引**：联合查询条件
- **覆盖索引**：包含查询所需的所有字段

---

## 四、API 接口设计

### 4.1 RESTful API 设计规范

**命名规范：**
```
GET    /api/users          # 获取用户列表
GET    /api/users/{id}     # 获取单个用户
POST   /api/users          # 创建用户
PUT    /api/users/{id}     # 更新用户
DELETE /api/users/{id}     # 删除用户
```

**HTTP 状态码：**

| 状态码 | 含义 | 使用场景 |
|--------|------|----------|
| 200 | OK | 请求成功 |
| 201 | Created | 资源创建成功 |
| 400 | Bad Request | 请求参数错误 |
| 401 | Unauthorized | 未授权 |
| 403 | Forbidden | 禁止访问 |
| 404 | Not Found | 资源不存在 |
| 500 | Internal Server Error | 服务器内部错误 |

### 4.2 响应格式统一

**成功响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "username": "john",
    "email": "john@example.com"
  },
  "timestamp": 1699900800000
}
```

**失败响应：**
```json
{
  "code": 400,
  "message": "参数校验失败",
  "errors": [
    {
      "field": "email",
      "message": "邮箱格式不正确"
    }
  ],
  "timestamp": 1699900800000
}
```

### 4.3 API 版本控制

```
/api/v1/users          # 版本1
/api/v2/users          # 版本2
```

**策略：**
- **URL 路径版本**：简单直观
- **Header 版本**：Accept: application/vnd.example.v1+json
- **Query 参数版本**：/api/users?version=1

### 4.4 完整 CRUD 示例

**用户管理 API：**

| API | 方法 | 描述 |
|-----|------|------|
| `/api/users` | GET | 获取用户列表（支持分页） |
| `/api/users/{id}` | GET | 获取单个用户 |
| `/api/users` | POST | 创建用户 |
| `/api/users/{id}` | PUT | 更新用户信息 |
| `/api/users/{id}` | DELETE | 删除用户 |

**创建用户请求体：**
```json
{
  "username": "string (必填，3-20字符)",
  "email": "string (必填，邮箱格式)",
  "password": "string (必填，6-32字符)",
  "nickname": "string (选填)",
  "phone": "string (选填，手机号格式)"
}
```

---

## 五、核心功能实现

### 5.1 用户认证与授权

**JWT Token 认证流程：**

```
登录请求 → 验证用户名密码 → 生成JWT Token → 返回Token
     ↓                         ↓
  后续请求                   验证Token
     ↓                         ↓
  携带Token                  解析Token
     ↓                         ↓
  验证Token                  获取用户信息
     ↓                         ↓
  授权成功                   返回用户上下文
```

**JWT 工具类：**

```java
@Component
public class JwtUtil {
    
    private static final String SECRET = "your-secret-key";
    private static final long EXPIRATION_TIME = 86400000; // 24小时
    
    public String generateToken(String username) {
        Date now = new Date();
        Date expiryDate = new Date(now.getTime() + EXPIRATION_TIME);
        
        return Jwts.builder()
                .setSubject(username)
                .setIssuedAt(now)
                .setExpiration(expiryDate)
                .signWith(SignatureAlgorithm.HS512, SECRET)
                .compact();
    }
    
    public String extractUsername(String token) {
        Claims claims = Jwts.parser()
                .setSigningKey(SECRET)
                .parseClaimsJws(token)
                .getBody();
        
        return claims.getSubject();
    }
    
    public boolean validateToken(String token) {
        try {
            Jwts.parser().setSigningKey(SECRET).parseClaimsJws(token);
            return true;
        } catch (JwtException | IllegalArgumentException e) {
            return false;
        }
    }
}
```

### 5.2 参数校验

**使用 Bean Validation：**

```java
public class CreateUserRequest {
    
    @NotBlank(message = "用户名不能为空")
    @Size(min = 3, max = 20, message = "用户名长度必须在3-20之间")
    private String username;
    
    @NotBlank(message = "邮箱不能为空")
    @Email(message = "邮箱格式不正确")
    private String email;
    
    @NotBlank(message = "密码不能为空")
    @Size(min = 6, max = 32, message = "密码长度必须在6-32之间")
    private String password;
    
    // getter/setter
}
```

**全局异常处理：**

```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    
    @ExceptionHandler(MethodArgumentNotValidException.class)
    public ResponseEntity<ErrorResponse> handleValidationException(
            MethodArgumentNotValidException ex) {
        
        List<FieldError> errors = ex.getBindingResult().getFieldErrors();
        List<ErrorDetail> errorDetails = errors.stream()
                .map(error -> new ErrorDetail(error.getField(), error.getDefaultMessage()))
                .collect(Collectors.toList());
        
        ErrorResponse response = new ErrorResponse(400, "参数校验失败", errorDetails);
        return ResponseEntity.badRequest().body(response);
    }
    
    @ExceptionHandler(BusinessException.class)
    public ResponseEntity<ErrorResponse> handleBusinessException(BusinessException ex) {
        ErrorResponse response = new ErrorResponse(ex.getCode(), ex.getMessage(), null);
        return ResponseEntity.status(ex.getCode()).body(response);
    }
}
```

### 5.3 业务逻辑层

**Service 层设计：**

```java
@Service
public class UserServiceImpl implements UserService {
    
    @Autowired
    private UserRepository userRepository;
    
    @Autowired
    private PasswordEncoder passwordEncoder;
    
    @Override
    @Transactional
    public User createUser(CreateUserRequest request) {
        // 检查用户名是否已存在
        if (userRepository.existsByUsername(request.getUsername())) {
            throw new BusinessException(400, "用户名已存在");
        }
        
        // 检查邮箱是否已存在
        if (userRepository.existsByEmail(request.getEmail())) {
            throw new BusinessException(400, "邮箱已被注册");
        }
        
        // 创建用户
        User user = new User();
        user.setUsername(request.getUsername());
        user.setEmail(request.getEmail());
        user.setPassword(passwordEncoder.encode(request.getPassword()));
        user.setNickname(request.getNickname());
        user.setPhone(request.getPhone());
        
        return userRepository.save(user);
    }
    
    @Override
    public User getUserById(Long id) {
        return userRepository.findById(id)
                .orElseThrow(() -> new BusinessException(404, "用户不存在"));
    }
    
    @Override
    @Transactional
    public User updateUser(Long id, UpdateUserRequest request) {
        User user = getUserById(id);
        
        if (request.getNickname() != null) {
            user.setNickname(request.getNickname());
        }
        if (request.getPhone() != null) {
            user.setPhone(request.getPhone());
        }
        
        return userRepository.save(user);
    }
    
    @Override
    @Transactional
    public void deleteUser(Long id) {
        User user = getUserById(id);
        userRepository.delete(user);
    }
}
```

### 5.4 数据访问层

**Repository 层：**

```java
public interface UserRepository extends JpaRepository<User, Long> {
    
    Optional<User> findByUsername(String username);
    
    boolean existsByUsername(String username);
    
    boolean existsByEmail(String email);
    
    Page<User> findByStatus(Integer status, Pageable pageable);
}
```

---

## 六、安全性考虑

### 6.1 密码安全

```java
@Configuration
public class SecurityConfig {
    
    @Bean
    public PasswordEncoder passwordEncoder() {
        // 使用 BCrypt 加密
        return new BCryptPasswordEncoder();
    }
}
```

**密码处理流程：**
```
用户注册 → 密码明文 → BCrypt加密 → 存储到数据库
    ↓
用户登录 → 密码明文 → BCrypt验证 → 验证成功/失败
```

### 6.2 防止 SQL 注入

**安全做法：**
```java
// ✅ 安全：使用参数化查询（JPA/Hibernate）
@Query("SELECT u FROM User u WHERE u.username = :username")
User findByUsername(@Param("username") String username);

// ✅ 安全：使用 Spring Data JPA 方法命名
User findByUsername(String username);

// ❌ 危险：字符串拼接
// String sql = "SELECT * FROM users WHERE username = '" + username + "'";
```

### 6.3 防止 XSS 攻击

```java
// 使用 @Valid 和 @SafeHtml 注解
@SafeHtml(message = "内容包含非法字符")
private String content;

// 或在输出时进行 HTML 转义
String safeContent = HtmlUtils.htmlEscape(userInput);
```

### 6.4 限流与熔断

**使用 Bucket4j 进行限流：**

```java
@Configuration
public class RateLimitConfig {
    
    @Bean
    public Bucket bucket() {
        Refill refill = Refill.greedy(100, Duration.ofMinutes(1));
        Bandwidth limit = Bandwidth.classic(100, refill);
        return Bucket.builder().addLimit(limit).build();
    }
}
```

---

## 七、部署与运维

### 7.1 Docker 容器化

**Dockerfile：**

```dockerfile
FROM openjdk:21-jdk-slim

WORKDIR /app

COPY target/backend-1.0.0.jar app.jar

EXPOSE 8080

ENTRYPOINT ["java", "-jar", "app.jar"]
```

**Docker Compose：**

```yaml
version: '3.8'
services:
  backend:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - DB_URL=jdbc:mysql://mysql:3306/example_db
      - DB_USER=admin
      - DB_PASSWORD=password
    depends_on:
      - mysql
      - redis

  mysql:
    image: mysql:8.0
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=example_db
      - MYSQL_USER=admin
      - MYSQL_PASSWORD=password
    volumes:
      - mysql_data:/var/lib/mysql

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data

volumes:
  mysql_data:
  redis_data:
```

### 7.2 环境配置

**application.yml：**

```yaml
server:
  port: 8080

spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

---
# 开发环境
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:mysql://localhost:3306/example_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

---
# 生产环境
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
```

### 7.3 健康检查与监控

**添加 Actuator 依赖：**

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-actuator</artifactId>
</dependency>
```

**监控端点：**

| 端点 | 描述 |
|------|------|
| `/actuator/health` | 健康检查 |
| `/actuator/metrics` | 指标监控 |
| `/actuator/info` | 应用信息 |
| `/actuator/loggers` | 日志管理 |

---

## 八、测试策略

### 8.1 测试分层

```
┌─────────────────────────────────────────────────────────────┐
│                    测试金字塔                               │
├──────────────┬──────────────┬──────────────┬──────────────┤
│   E2E测试    │   集成测试    │   单元测试    │   静态分析   │
│   (少量)     │   (中量)     │   (大量)     │   (持续)     │
│  UI/API测试  │  模块间交互  │  单个方法    │ 代码质量检查 │
└──────────────┴──────────────┴──────────────┴──────────────┘
```

### 8.2 单元测试示例

```java
@SpringBootTest
class UserServiceTest {
    
    @MockBean
    private UserRepository userRepository;
    
    @Autowired
    private UserService userService;
    
    @Test
    void createUser_Success() {
        CreateUserRequest request = new CreateUserRequest();
        request.setUsername("test");
        request.setEmail("test@example.com");
        request.setPassword("password");
        
        when(userRepository.existsByUsername(any())).thenReturn(false);
        when(userRepository.existsByEmail(any())).thenReturn(false);
        when(userRepository.save(any(User.class))).thenAnswer(invocation -> invocation.getArgument(0));
        
        User result = userService.createUser(request);
        
        assertNotNull(result);
        assertEquals("test", result.getUsername());
        verify(userRepository).save(any(User.class));
    }
    
    @Test
    void createUser_UsernameExists() {
        CreateUserRequest request = new CreateUserRequest();
        request.setUsername("existing");
        request.setEmail("test@example.com");
        request.setPassword("password");
        
        when(userRepository.existsByUsername("existing")).thenReturn(true);
        
        assertThrows(BusinessException.class, () -> userService.createUser(request));
    }
}
```

---

## 九、代码规范

### 9.1 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 类名 | 大驼峰 | `UserController`, `UserServiceImpl` |
| 方法名 | 小驼峰 | `createUser`, `getUserById` |
| 变量名 | 小驼峰 | `userName`, `orderList` |
| 常量名 | 全大写，下划线分隔 | `MAX_PAGE_SIZE`, `SECRET_KEY` |
| 包名 | 全小写 | `com.example.app.controller` |

### 9.2 代码风格

```java
// ✅ 良好的代码风格
public ResponseEntity<UserResponse> getUserById(@PathVariable Long id) {
    User user = userService.getUserById(id);
    UserResponse response = UserResponse.fromEntity(user);
    return ResponseEntity.ok(response);
}

// ✅ 使用 Optional 避免空指针
Optional<User> user = userRepository.findById(id);
user.ifPresent(u -> logger.info("Found user: {}", u.getUsername()));
```

---

## 十、总结

**后端工程开发完整流程：**

```
1. 需求分析 → 明确业务目标和功能范围
       ↓
2. 技术选型 → 选择合适的语言、框架、数据库
       ↓
3. 架构设计 → 分层架构、模块划分
       ↓
4. 数据库设计 → ER图、表结构、索引
       ↓
5. API设计 → RESTful规范、请求响应格式
       ↓
6. 代码实现 → Controller → Service → Repository
       ↓
7. 安全加固 → 认证授权、参数校验、防注入
       ↓
8. 测试验证 → 单元测试、集成测试
       ↓
9. 部署上线 → Docker、CI/CD
```

**关键产出物清单：**
- ✅ 需求文档（PRD）
- ✅ 技术方案文档
- ✅ ER图与数据库设计文档
- ✅ API接口文档
- ✅ 代码仓库
- ✅ 测试用例
- ✅ Docker配置
- ✅ CI/CD配置